[metadata]
name = rosbag-deck
version = 0.1.0
description = Python bindings for rosbag-deck

[options]
package_dir =
    = python
packages = rosbag_deck
zip_safe = False

[options.package_data]
rosbag_deck = *.so, *.pyd
