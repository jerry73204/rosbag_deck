[package]
name = "rosbag-deck"
description = "Interactive ROS 2 bag player with tape deck-style controls"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
mcap = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

# TODO: Add rosbag-deck-ffi dependency once ROS 2 FFI layer is ready
# rosbag-deck-ffi = { workspace = true }
